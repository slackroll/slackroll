
urls-key-packages

This operation expects no arguments. It's similar to the
"download-key-packages" operation, but the package archives and signatures will
not be downloaded. Instead, the program will simply print their URLs.

Also see: upgrade-key-packages, download-key-packages.
